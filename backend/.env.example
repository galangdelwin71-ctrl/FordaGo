APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=laravel
# DB_USERNAME=root
# DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# ── Email Providers (Resend / Brevo / SMTP) ───────────────────────────
# Tip for DigitalOcean/VPS: DigitalOcean blocks SMTP ports (25/465/587).
# Use RESEND_API_KEY or BREVO_API_KEY to send emails via HTTPS (Port 443) smoothly.
MAIL_FROM_ADDRESS="no-reply@fordago.com"
MAIL_FROM_NAME="FordaGO Gym"

# 1. Resend (Recommended: 3,000 free emails/month via HTTPS port 443)
RESEND_API_KEY=

# 2. Brevo (Alternative: 300 free emails/day via HTTPS port 443)
BREVO_API_KEY=

# 3. Standard SMTP (Fallback)
MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null

# ── SMS Providers (philsms / semaphore / twilio) ──────────────────────
SMS_PROVIDER=philsms

# PhilSMS (https://app.philsms.com)
PHILSMS_API_TOKEN=
PHILSMS_SENDER_ID=PhilSMS

# Semaphore (https://semaphore.co)
SEMAPHORE_API_KEY=
SEMAPHORE_SENDER=FordaGO

# Twilio
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# ── WebSocket & Pusher / Reverb ───────────────────────────────────────
REVERB_APP_ID=503802
REVERB_APP_KEY=vii4ztpeqxttvr8p001g
REVERB_APP_SECRET=zanrupfvnwsanmbvyywn
REVERB_HOST="localhost"
REVERB_PORT=8080
REVERB_SCHEME=http

VITE_REVERB_APP_KEY="${REVERB_APP_KEY}"
VITE_REVERB_HOST="${REVERB_HOST}"
VITE_REVERB_PORT="${REVERB_PORT}"
VITE_REVERB_SCHEME="${REVERB_SCHEME}"
